use syslib::*;
use shared::ipc_defs::*;
use mallot::*;
use file::*;
use ipclib::*;
use string::*;
use name::*;


fn main() -> i32 {

    mallot_init();
    let mb = mailbox_new();
    let fs_mb: i32;
    if ((fs_mb = file_server_init(5, mb)) < 0) {
        printf!("error\n");
        abscond(-1);
    } 
    printf!("ready\n"); 

    abscond(1);
    0
}
