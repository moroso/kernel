use syslib::*;
use shared::ipc_defs::*;
use mallot::*;
use file::*;
use ipclib::*;
use string::*;
use name::*;


fn main() -> i32 {

    mallot_init();
    let mb = mailbox_new();
    

    syslib::abscond(1);
    0
}
